spring:
  datasource:
      url: jdbc:mysql://localhost:3306/map
      username: root
      password: hibernate
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
